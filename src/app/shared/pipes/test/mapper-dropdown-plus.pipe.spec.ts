import { MapperDropdownPlusPipe } from '../mapper-dropdown-plus.pipe';

describe('MapperDropdownPlusPipe', () => {
  it('create an instance', () => {
    const pipe = new MapperDropdownPlusPipe();
    expect(pipe).toBeTruthy();
  });
});
