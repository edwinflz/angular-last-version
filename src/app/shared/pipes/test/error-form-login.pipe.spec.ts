import { ErrorFormLoginPipe } from '../error-form-login.pipe';

describe('ErrorFormLoginPipe', () => {
  it('create an instance', () => {
    const pipe = new ErrorFormLoginPipe();
    expect(pipe).toBeTruthy();
  });
});
