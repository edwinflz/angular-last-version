import { FilterDropdownPlusPipe } from '../filter-dropdown-plus.pipe';

describe('FilterDropdownPlusPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDropdownPlusPipe();
    expect(pipe).toBeTruthy();
  });
});
