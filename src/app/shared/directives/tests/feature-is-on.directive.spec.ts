import { FeatureIsOnDirective } from '../feature-is-on.directive';

describe('FeatureIsOnDirective', () => {
  it('should create an instance', () => {
    const directive = new FeatureIsOnDirective();
    expect(directive).toBeTruthy();
  });
});
