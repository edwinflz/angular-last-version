import { ApplyBgColorDirective } from '../apply-bg-color.directive';

describe('ApplyBgColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyBgColorDirective();
    expect(directive).toBeTruthy();
  });
});
