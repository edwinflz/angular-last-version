import { SwiperCustomDirective } from '../swiper-custom.directive';

describe('SwiperCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new SwiperCustomDirective();
    expect(directive).toBeTruthy();
  });
});
