import { environment as ENV } from '@environments/environment';

export const URLS_HTTP_LOADING_IGNORE = [
  '.json',
];

export const URLS_MOBILE = ['mobile', 'auth'];
export const URLS_FOOTER = ['auth'];

export const PATH_MOBILE = ENV.mobileBaseUrl;
