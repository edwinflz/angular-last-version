export const enum AuthUrlParameters {
  ACTION = 'action',
  TOKEN = 'token',
  USER_ID = 'user_id',
  FROM_WEBVIEW = 'fromMobile'
}

export const enum SubscriptionUrlParameters {
  JWT = 'jwtToken',
  REFRESH_TOKEN = 'refreshToken',
  EXPIRATION_TOKEN = 'expirationToken'
}
